Class access modifiers are keywords that allow you to control who has access to those classes.
They are:
- `public` -> they can be accessed from anywhere
- `internal` -> they can be accessed only from the containing [[Project|project]]
- `file` -> they can be accessed only from the containing file
```C#
public class ExampleClass
{

}
```