At the softwate level, the [[Computer (CPU)#RAM - Random Access Memory|memory]] is split into two types:
- The **Stack** - "used for static memory allocation"
- The **Heap** - "used for dynamic memory allocation"